#posts.transitions-enabled
	- @posts.each do |post|
		.box.panel.panel-default
			= link_to (image_tag post.image.url), post
			.panel-body
				%h2= link_to post.description, post
				%p.user
				Submitted by
				=post.user.email